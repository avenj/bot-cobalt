#!perl

use 5.12.1;
use strict;
use warnings;
use Carp;

my $path_confsdir;
my $path_rcfile;

use Getopt::Long;
GetOptions(
  help => sub {
  
    exit 0
  },
  
  'confsdir=s' => \$path_confsdir,
  
  'rcfile=s'   => \$path_rcfile;
  
  ## FIXME
);

## FIXME
## try to install .conf files from 'confs/' in either specified libdir
## or found libdir
## if the .conf already exists, show diff, offer to exit or backup+replace
